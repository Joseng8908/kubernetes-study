apiVersion: v1
kind: Service
metadata:
  name: k8s-demo-service
spec:
  type: NodePort
  selector:
    app: k8s-demo
  ports:
    - protocol: TCP
      port: 80      # 서비스 포트
      targetPort: 8080 # 컨테이너 포트
      nodePort: 30080 # 로컬에서 접속할 포트
